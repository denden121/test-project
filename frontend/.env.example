# URL бэкенда (на Vercel укажите URL вашего API, например Railway/Render)
VITE_API_URL=/api

# Google OAuth: Client ID (только ID, без секрета). Для кнопки «Войти через Google».
# В Google Cloud Console в Authorized redirect URIs укажите: https://ВАШ-ДОМЕН/auth/callback
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Куда проксировать /api в dev (по умолчанию http://localhost:8000)
# VITE_PROXY_TARGET=http://localhost:8000
