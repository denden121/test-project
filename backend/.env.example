# PostgreSQL (asyncpg). Пример для локальной разработки и для Vercel Postgres / Neon / Supabase
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/test_project

# Google OAuth (опционально). Если заданы — на странице входа появится «Войти через Google».
# В Google Cloud Console добавьте Authorized redirect URI: https://ВАШ-ДОМЕН/auth/callback
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...

# Папка для загруженных аватаров (по умолчанию uploads)
# UPLOAD_DIR=uploads

# Восстановление пароля: URL фронта для ссылки в письме
# FRONTEND_URL=http://localhost:5173
# Resend (если задан — при «забыли пароль» отправляется письмо).
# onboarding@resend.dev только для теста: письма доходят только на email вашего аккаунта Resend.
# Чтобы отправлять любым получателям: в Resend добавьте и верифицируйте свой домен (DNS),
# затем укажите адрес с этого домена, например noreply@ваш-домен.com
# RESEND_API_KEY=re_...
# RESEND_FROM_EMAIL=onboarding@resend.dev
# RESEND_FROM_EMAIL=noreply@ваш-домен.com
