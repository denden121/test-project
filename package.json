{
  "name": "test-project",
  "private": true,
  "type": "module",
  "description": "Root for scripts (Sonar, etc.)",
  "scripts": {
    "sonar": "node scripts/sonar.js"
  },
  "devDependencies": {
    "sonarqube-scanner": "^3.3.0"
  }
}
